import 'package:flutter/material.dart';

/// Placeholder — full curriculum CRUD + AI helper panel, owned by the
/// CMS/dashboard feature workstream.
class CmsHomeScreen extends StatelessWidget {
  const CmsHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Content management coming soon.'));
  }
}
