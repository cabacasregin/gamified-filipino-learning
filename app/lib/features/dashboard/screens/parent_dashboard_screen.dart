import 'package:flutter/material.dart';

/// Placeholder — per-child progress dashboard, owned by the
/// CMS/dashboard feature workstream.
class ParentDashboardScreen extends StatelessWidget {
  const ParentDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Children progress dashboard coming soon.'));
  }
}
