import 'package:flutter/material.dart';

/// Placeholder — class-wide progress dashboard, owned by the
/// CMS/dashboard feature workstream.
class TeacherDashboardScreen extends StatelessWidget {
  const TeacherDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Class progress dashboard coming soon.'));
  }
}
