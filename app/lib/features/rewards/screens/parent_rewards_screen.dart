import 'package:flutter/material.dart';

/// Placeholder — parent reward catalog management + redemption approvals,
/// owned by the rewards feature workstream.
class ParentRewardsScreen extends StatelessWidget {
  const ParentRewardsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Reward management coming soon.'));
  }
}
