import 'package:flutter/material.dart';

/// Placeholder — student-facing points wallet + reward catalog +
/// redemption requests, owned by the rewards feature workstream.
class RewardsScreen extends StatelessWidget {
  const RewardsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Rewards catalog coming soon.'));
  }
}
